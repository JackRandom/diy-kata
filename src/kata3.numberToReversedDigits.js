const numberToReversedDigits = number => {

    // String(number).split('').reverse();
//     number = number + '';
//     let reversedText = number.split('').reverse().join('');
//     let reversedNumber = parseInt(reversedText, 10);
// console.log("reversed number: ", reversedNumber);
//     return reversedNumber;
    
    //   return number.toString().split('').reverse(x => parseInt(x)).map(Number);


    // const array = number.toString().split("");
  	//  return array.reverse(x => parseInt(x));

};

module.exports = numberToReversedDigits;

// const numberToReversedDigits = number => String(number).split('').reverse();

// module.exports = numberToReversedDigits;